# Triage labels

The repository uses the default triage label vocabulary:

- `needs-triage`
- `needs-info` 
- `ready-for-agent`
- `ready-for-human`
- `wontfix`

These labels correspond to the five canonical triage roles and are used by the `triage` skill.