# 11 — Establish repository hygiene for generated output, .DS_Store, secrets, and local environment files

**What to build:** Clean repository setup that avoids committing unwanted files.

**Blocked by:** 02 — Create solution/project scaffolding and dependency pinning

**Status:** ready-for-agent

- [ ] Create `.dockerignore` with appropriate exclusions
- [ ] Add entries to `.gitignore` for generated files, .DS_Store, secrets
- [ ] Set up `.env.example` with placeholder values
- [ ] Configure local environment files exclusion