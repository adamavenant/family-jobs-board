# 08 — Create multi-stage, non-root production Dockerfiles compatible with Apple Silicon and Linux

**What to build:** Production-ready Docker images that support multiple architectures.

**Blocked by:** 02 — Create solution/project scaffolding and dependency pinning

**Status:** ready-for-agent

- [ ] Create multi-stage Dockerfiles for API and Web containers
- [ ] Configure non-root user execution in production containers
- [ ] Ensure ARM64 (Apple Silicon) and AMD64 (Linux) compatibility
- [ ] Test image builds on both architectures